# Naming Conventions

- Use `kebab-case` for folders and docs.
- Use `PascalCase` for React components and exported types when needed.
- Use descriptive capability-based package names.
- Keep workspace package names under the `@talentone/*` namespace.