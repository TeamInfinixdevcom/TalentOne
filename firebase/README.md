# Firebase Foundation

This directory is reserved for Firebase project structure only.

It will later contain authentication, Firestore, storage, hosting, functions, rules, indexes, and emulator configuration.